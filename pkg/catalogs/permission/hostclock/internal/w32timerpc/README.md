# Local Windows time-service queries

This package owns the bounded W32Time QueryStatus exchange.
The Windows connector checks the pipe server against the running service process before it sends a request.
`QueryStatus` accepts an already checked stream, owns it, and closes it after the exchange.

The RPC client receives one fixed stream through its dialer hook. It cannot select another address or reconnect.
The reader limits each fragment to 16 KiB and the full reply to 64 KiB.
It checks allocation hints before decoding and preserves the observation deadline when the client changes stream deadlines.

The connector reads service state and uses the local `W32TIME_ALT` pipe with caller identification.
It grants no permission to act as the caller. It starts no service and changes no time setting.
Cancellation bounds the caller's wait. One process-wide slot limits native service-manager calls that cannot accept a context.
That slot remains occupied until the outstanding query returns and releases its resources.

This package reports status. The parent hostclock package owns source profiles, timestamp units, and UTC uncertainty.

<!-- gomarkdoc:embed:start -->

<!-- Code generated by gomarkdoc. DO NOT EDIT -->

# w32timerpc

```go
import "github.com/agentstation/starmap/pkg/catalogs/permission/hostclock/internal/w32timerpc"
```

Package w32timerpc bounds read\-only status queries to the local Windows time service. Callers must check the peer before supplying a stream to QueryStatus.

## Index

- [func ObserveStatus\(ctx context.Context\) \(\*w32t.StatusInfo, error\)](<#ObserveStatus>)
- [func QueryStatus\(ctx context.Context, raw net.Conn\) \(\*w32t.StatusInfo, error\)](<#QueryStatus>)


<a name="ObserveStatus"></a>
## func [ObserveStatus](<https://github.com/agentstation/starmap/blob/main/pkg/catalogs/permission/hostclock/internal/w32timerpc/observation.go#L54>)

```go
func ObserveStatus(ctx context.Context) (*w32t.StatusInfo, error)
```

ObserveStatus reads the local W32Time service without changing its settings.

<a name="QueryStatus"></a>
## func [QueryStatus](<https://github.com/agentstation/starmap/blob/main/pkg/catalogs/permission/hostclock/internal/w32timerpc/query.go#L44>)

```go
func QueryStatus(ctx context.Context, raw net.Conn) (*w32t.StatusInfo, error)
```

QueryStatus reads W32Time status from a stream. The caller must check its peer first. It owns and closes the stream. It invokes only the generated QueryStatus operation.

Generated by [gomarkdoc](<https://github.com/princjef/gomarkdoc>)


<!-- gomarkdoc:embed:end -->
