//go:generate gomarkdoc -e -f github -o docs/API.md . --repository.url https://github.com/agentstation/starmap --repository.default-branch main --repository.path /

// Package starmap provides immutable AI model catalog reads, explicit
// generation publication, event hooks, and caller-selected generation storage.
package starmap
